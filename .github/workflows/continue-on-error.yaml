name: continue-on-error
on:
  workflow_dispatch:
  pull_request:

jobs:
  context:
    runs-on: ubuntu-20.04
    steps:
    - name: Dump Context
      continue-on-error: true
      run: exit 1
